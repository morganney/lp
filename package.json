{
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp landing-page.html dist/index.html && cp styles.css script.js dist && cp -R assets dist/assets",
    "serve": "npm run build && serve -l 4174 dist",
    "format": "oxfmt"
  },
  "devDependencies": {
    "oxfmt": "^0.67.0",
    "serve": "^14.2.6"
  }
}
